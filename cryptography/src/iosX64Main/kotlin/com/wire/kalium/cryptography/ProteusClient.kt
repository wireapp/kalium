package com.wire.kalium.cryptography

import platform.Foundation.*
import com.wire.WireCryptobox.*

actual class ProteusClient {
    actual fun hello() {
        val url = NSURL(string = "file://this")
        val e = EncryptionContext()
//        AFURLSessionManager(null)
    }
}
