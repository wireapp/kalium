Added `SyncRequest.waitUntilNextLiveOrFailure()` to await the next successful sync.

  - ABI: additive
  - Source: additive
  - Behavior: no behavior change unless consumers call the new sync waiting API.
