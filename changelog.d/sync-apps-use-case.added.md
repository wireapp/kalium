Added `AppScope.syncApps` with `SyncAppsUseCase` to synchronize apps available to the current user's team.

  - ABI: additive.
  - Source: additive.
  - Behavior: no behavior change unless consumers call the new app synchronization API.
  - Migration: no action required.
