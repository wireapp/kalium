### Added
- Added `SendPendingMessagesUseCase` to send pending messages on demand.
- Added `SyncRequest.waitUntilNextLiveOrFailure()` to await the next successful sync.

### Migration
No action required.

### Compatibility
ABI: additive.
Source: additive.
Behavior: no behavior change for existing consumers.
